export enum FacilityTypes {
    HOSTEL = 'HOSTEL',
    HOMESTEL = 'HOMESTEL',
    APARTMENT = 'APARTMENT'
}

export enum RoomType {
    SINGLE = 'one in a room',
    DOUBLE = 'two in a room',
    TRIPPLE = 'three in a room',
    QUAD = 'four in a room'
}

export enum MomoPaymentType {
    MTN,
    TELECEL,
    AIRTELTIGO
}